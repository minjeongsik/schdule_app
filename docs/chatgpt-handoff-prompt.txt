You are helping continue development of a map-based scheduler MVP.

Read this summary first, then continue from the current state instead of proposing a rewrite.

What matters most

1. Keep the project MVP-level and beginner-friendly.
2. Avoid overengineering.
3. Preserve the current structure unless there is a strong reason to change it.
4. Prefer incremental changes with small commits.
5. Treat `todo` code as legacy unless a task explicitly asks to revive or delete it.

Current stack

- Backend: Express + TypeScript + Prisma + SQLite
- Frontend: React + TypeScript + Vite
- Data fetching: React Query
- Validation: Zod

Current domain rules

- Place is user-scoped
- Appointment has many route candidates
- Route candidate has many waypoints
- Frontend error handling is split into `field / form / page`

Current implemented state

- Places CRUD exists
- Appointments CRUD exists
- Route candidate create / select / update / delete exists
- Waypoints are stored per route candidate
- Selected route is shown in the UI
- Route candidate management is inside the appointment dashboard UI
- `todo` runtime routes are not connected

Recent QA and exception-handling improvements

- Server tests were added for risky route flows
- Client tests now cover `page`, `form`, and `field` error rendering
- Route form now validates positive numbers before submit
- Route waypoint lines are validated in the frontend before submit
- Route action failures are shown inside the route section
- Page errors now indicate which resource failed to load

Current backend structure

```text
server/
  prisma/
    schema.prisma
    seed.ts
  src/
    app.ts
    index.ts
    config/
      env.ts
    lib/
      prisma.ts
    middlewares/
      error-handler.ts
      request-context.ts
    modules/
      appointments/
        appointments.controller.ts
        appointments.routes.ts
        appointments.schema.ts
        appointments.service.ts
      places/
        places.controller.ts
        places.routes.ts
        places.schema.ts
        places.service.ts
    test/
      appointments-routes.test.ts
    utils/
      http-error.ts
      logger.ts
```

Current frontend structure

```text
client/
  src/
    api/
      appointments.ts
      client.ts
      places.ts
    hooks/
      use-scheduler.ts
    pages/
      SchedulerDashboardPage.tsx
    test/
      SchedulerDashboardPage.test.tsx
      server.ts
      setup.ts
    types/
      scheduler.ts
    App.tsx
    main.tsx
    styles.css
```

Important current APIs

```http
GET    /api/appointments?userId=demo-user
GET    /api/appointments/:id
POST   /api/appointments
PATCH  /api/appointments/:id
DELETE /api/appointments/:id

POST   /api/appointments/:id/routes
PATCH  /api/appointments/:id/routes/:routeId
PATCH  /api/appointments/:id/routes/:routeId/select
DELETE /api/appointments/:id/routes/:routeId

GET    /api/places?userId=demo-user
POST   /api/places
PATCH  /api/places/:id
DELETE /api/places/:id?userId=demo-user
```

Important current Prisma facts

- `Place.userId` exists
- `SavedRoute` is still the model name used for route candidates
- `Waypoint` model exists and is related to `SavedRoute`

Important local environment note

- The correct SQLite path is `file:./prisma/dev.db`
- `server/.env` and `server/.env.example` must use:

```env
DATABASE_URL="file:./prisma/dev.db"
```

- A previous bug came from using the wrong path (`file:./dev.db`)
- If the app shows `Internal server error` after env changes, restart the server process
- `GET /api/health` should show a database URL ending with `/server/prisma/dev.db`

Current QA and docs status

- `docs/qa-checklist.md` is aligned to scheduler flows
- `docs/user-guide.md` explains route waypoint input format
- `docs/debugging-guide.md` includes `/api/health` and DB-path checks
- `docs/plans/20260326_102141_scheduler-qa-plan.txt` stores the current QA hardening plan

Known weak points

- There is still no auth layer; `userId` is still request-driven
- Route waypoint editing is still full-replace, not granular
- `todo` files still exist in the repo as legacy noise
- The local bad file `server/dev.db` may exist from an old path mistake but should not be used

Known legacy status

- `todo` files still exist in the repo
- Current runtime uses scheduler modules, not todo routes
- Do not remove or revive `todo` unless the task explicitly asks for it

Current verification status

- server lint/build/test passes
- client lint/test/build passes
- local DB path issue was fixed and reseeded

Recommended mindset for the next assistant

- Do not suggest a large architecture rewrite
- Do not add a repository layer just for abstraction
- Do not split the frontend into many files unless a clear pain point appears
- Prefer improving scheduler behavior, validation, testing, and UX incrementally

If you continue from here, structure your response as:

1. What is already solid
2. What is still weak or risky
3. The next smallest useful implementation step
4. What should wait until later
